spring:
  config:
    import:
      - application-jpa.yml
  batch:
    job:
      name: ${job.name:NONE}
  redis:
    host: localhost
    port: 6379
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
